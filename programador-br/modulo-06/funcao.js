function calcularMedia(nota1, nota2) {
    let media = (nota1 + nota2) / 2;
    return media;
}

console.log("A média é: " + calcularMedia(10, 8.5));

let media = function(n1, n2) {
    return (n1 + n2) / 2;
}

console.log(media(5, 6));

let calcMedia = (num1, num2) => {
    return (num1 + num2) / 2;
}